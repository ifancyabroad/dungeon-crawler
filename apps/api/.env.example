PORT=4000
MONGO_URI=
WEB_ORIGIN=

# Required in production. Leave empty in dev/test (warning will be logged).
GAME_TOKEN_PEPPER=

# Optional. Set to a strong random string to enable debug API routes (POST /api/debug/*).
# Leave unset in production. Must match VITE_DEBUG_SECRET in the web app.
DEBUG_SECRET=